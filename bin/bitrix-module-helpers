#!/usr/bin/env bash

PHP_BIN=$(which php)

if [ -z "$PHP_BIN" ]; then
    echo "Не удалось найти php" >&2
    exit 1
fi

SCRIPT_PATH=$(readlink -f "$0")
SCRIPT_DIR=$(dirname "$SCRIPT_PATH")

exec "$PHP_BIN" "$SCRIPT_DIR/bitrix-module-helpers.php" "$@"
