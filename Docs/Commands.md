# Команды для управления созданием модулей

После `composer install` они доступны в общей bin-папке:

```
vendor/bin/bitrix-module-helpers <команда>
```

- [module:new](Commands/NewModule.md) — создание нового модуля (обычного или дочернего)
